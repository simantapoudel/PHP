<?php 
    /* 
        Different types of operators 
            Arithmetic operators: +,-,*,/,%,**
            Assignment operators: =, +=, -=, *=, /=, %=
            Comparison operators: ==, ===, <=, >=, <, >, !=, <>, !==, <=>
            Increment/Decrement operators: ++$x, --$x, $x++, $x--
            Logical Operators: and, &&, or, ||, xor, !
            String Operators: .(concatenation) and .=(concatenation assignment)
            Conditional Operators: ?:, ??
            ?: (Ternary Operator) 

            Example of ternary operator ?: 
            $x = 14;
            $check = ($x < 12) ? "less than 12" : "greater than 12";
            echo $check;

            Example of ?? (Null Coalescing Operator)

            $user = 'simanta';
            $status = $user ?? 'Register';
            echo $status;

            Example of . operator: 
            $fname = 'Simanta';
            $lname = 'Poudel';
            echo $fname . " " . $lname;

            Example of .= operator: 
            $fname = 'Simanta ';
            $lname = 'Poudel';
            $fname .= $lname;
            echo $fname;
    */
    // $odd = [1,3,5,7,9];
    // $even = [2,4,6,8,10];
    // $numbers  = $odd + $even;
    // print_r($numbers);
   
    
?>